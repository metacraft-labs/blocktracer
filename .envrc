# The flake devShell is the project environment (nim, nimble, just, python3,
# wrangler, plus ISONIM_SRC / NIM_EVERYWHERE_SRC / DESIGN_SYSTEM_SRC from the
# pinned inputs). `direnv allow` once, and every command in this directory runs
# with the same toolchain CI uses.
use flake
