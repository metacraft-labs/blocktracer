{
  "_comment": [
    "Journeys known to be RED on this branch, with the reason and the work that closes each.",
    "",
    "THIS FILE FAILS IN BOTH DIRECTIONS. An entry does not silence a journey — the journey",
    "still runs, still prints every failed assertion, and is still counted. What the entry does",
    "is stop the exit code from blocking every other branch on a defect that is already known",
    "and already stated. And a ledgered journey that goes GREEN fails the run, so an entry",
    "cannot outlive its defect: whoever lands the fix is told, by name, to delete the line.",
    "",
    "An entry is only legitimate with a `closed_by` that names real work and an `evidence` line",
    "that says what was measured. A ledger entry written to make a suite green is the defect",
    "this whole layer exists to catch, wearing the suite's own uniform.",
    "",
    "One entry. It is a product decision rather than a bug with an obvious fix, and this layer found it."
  ],
  "known_red": {
    "a-value-can-be-traced-to-its-origin": {
      "reason": "No control in ANY session offers to trace a value to its origin — on the demo chain or on a chain capture. What remains red is that one claim, now asserted on both arms ('some control offers to trace a value to its origin' and 'REAL: a chain capture offers no way to trace a value to its origin either'); the other two halves of this journey are green and neither should be re-diagnosed from memory. The COPY half was resolved first — the promise was removed from home.nim and ssr.nim, and this journey asserts its absence. The LIVE-VALUE half was resolved next: the entry here used to say the blocker was upstream, in the pinned Embed SDK, because `ReplayDataStore.requestLocals` (store/replay_data_store.nim:628-681) sends `ct/load-locals` and discards the reply through a private `onComplete` wrapper (:75-82) that exists to drop the result value. That is still true of the pinned store and it is NOT the blocker it was taken to be: the reply is readable on the seam this repository owns. `client/hydrate/live_locals.nim` decorates the BackendService `openLiveSession` builds the store on, so the ViewModel's own request passes through it and its own reply is parsed into `store.updateLocals` — one request, one reply, one writer, no pin bump. A hydrated session now shows the engine's values at the position it is at, and says so honestly when there are none. So `StateVM.currentVariables` is populated, `projectState` yields rows and the State pane's latch fires. What is left is the origin surface itself: BlockTracer sends neither `ct/originChain` nor `ct/originSummary`, constructs no OriginChainVM, and renders no affordance on a State row. A second blocker outlives that one for CHAIN transactions specifically — every published one is declared rung 3, and demo_session.nim states the consequence verbatim: 'This recording carries no variable names: naming a local needs debug symbols, which an Aztec contract class does not publish.' The origin classifier works by splitting the RHS of a source assignment, so with no source and no names it terminates at UnknownVariable on every hop. The demo tour's Noir programs do not have that problem and are where the surface should first be demonstrated.",
      "evidence": "dev @ 2fcbc99 plus this branch, measured in a browser over `just export-hydrated` with the replay engine staged. THE JOURNEY NOW JUDGES BOTH KINDS OF RECORDING, which it did not before: it selected its subject as `sessions.find((t) => !t.real) ?? sessions[0]`, which prefers a synthetic fixture, and with 15 synthetic sessions in the corpus the fallback arm could never run — so the 'counted 0' this entry was written from had only ever been measured on the demo chain. It is now two subject lists selected by filter, each asserted non-empty with its count printed (15 synthetic, 8 real), both driven, no `??` between them. Measured: DEMO — 83 interactive controls, phase=ready, 0 origin affordances; CHAIN capture /aztec-testnet/tx/0x0858d6…/debug — 19 interactive controls, phase=ready, 0 origin affordances. AND THE FIRST RUN THAT REACHED A CHAIN CAPTURE COUNTED 1, NOT 0, WHICH WAS A MEASUREMENT ARTEFACT AND IS RECORDED AS ONE: the probe matched its regex against each candidate's whole `textContent`, and an event-log row on all eight real captures flattens to '…avm:11912 status=unavailable-in-principle origin=settled-chain…'. `origin=` there is a field of the event the chain recorded. Because the assertion asserts PRESENCE (`atLeast(…, 1)`) that would have been a FALSE GREEN, and on a ledgered known-red journey a false green fails the run demanding this entry be deleted — over an event-log string. The probe now matches authored labels (`data-action`, `aria-label`, `title`, or text short enough to be a label) and REPORTS the wide count beside the verdict; on the chain capture the two read '0 labelled, 1 matched anywhere', so the artefact stays visible instead of being silently narrowed away. Journey 11 walks the same demo session across 14 positions and reads 3 distinct sets of values and 69 engine-supplied rows, and — also new — walks a CHAIN capture across 15 positions for 14 distinct sets and 70 rows, none blank, none the served frame's. `grep -r 'OriginChain|origin_chain|originChain'` over all of blocktracer still returns 0 hits. The engine's own `ct/originChain` and `ct/originSummary` dispatch arms exist (db-backend dap_server.rs:2085/:2090, not cfg-gated, so they are in the wasm build blocktracer downloads) and blocktracer sends neither. Note also what the load-locals reply already carries and nothing reads: every local comes back with an `originSummary` ({confidence, hopCount, isPlaceholder, placeholderToken, terminatorExpr, terminatorFunction, terminatorKind}) — observed on the wire, not inferred — and `StateVM.originSummaries` is the signal spec §3.2.3 says it populates.",
      "closed_by": "UNOWNED, and now one repository rather than two. Step (1) of the previous plan — 'upstream, in codetracer, parse the ct/load-locals reply into updateLocals' — is DONE, on this side of the pin, and steps (2) 'bump ci/embed-sdk-pin.env' and its premise are withdrawn: the pin does not need to move for this. What remains, in order: (a) carry the `originSummary` the reply already delivers into `LocalsFeed`/`StateVM.originSummaries` — no new request, it is already on the wire; (b) construct an OriginChainVM in openLiveSession and route `ct/updated-origin-chain` in hydrate.nim's onDapEvent as desktop does in ui_js.nim:1866-1905; (c) model origin on StateValue and draw the badge and the in-row hop chain in renderState; (d) surface the four-state honesty indicator desktop already has — StateVM.originMetadataMode ('on'/'lazy'/'off'/'unavailable', defaulting to unavailable) — so a recording that cannot support a chain says so per transaction rather than showing an empty panel. Demonstrate it FIRST on the demo tour's Noir programs, which have sources and varnames; a rung-3 chain transaction cannot support a meaningful chain no matter what is wired."
    }
  },
  "_closed": {
    "_comment": [
      "Not machine-read. Kept because the both-directions rule paid for itself three times in",
      "two days, and the record is worth more than the lines it costs. Every entry here was",
      "removed because the run FAILED demanding its removal — nobody had to remember."
    ],
    "a-failed-execution-is-tellable-from-a-completed-one": {
      "was": "The tour's `constraints` program stops on an assertion that cannot hold \u2014 170 against a floor of a million \u2014 and its recording ends there. The page presented it identically to a program that ran to completion: both carried the badges 'Succeeded | Synthetic demo data | No timestamp published | Yes | Finalized | Trace ready' and the same provenance sentence, and nothing anywhere said the execution did not finish. A visitor could not tell the two apart without reading the Noir source in the pane.",
      "cause": "THE FACT WAS NOT PUBLISHED, anywhere. `ExecutionSummary` (src/blocktracer/contract/model.nim) carried steps, frames, truncated, sourceLevel and languages \u2014 five keys, none of which is an ending. `truncated` is the nearest neighbour and answers the opposite question: a truncated recording is one whose ending is MISSING. The only place the fact existed was `fixtures/trace/tour/manifest.json`, in the `failure` capability, and the generator read that key only into `g/{gen}/tour.json`, which the transaction and debug routes never open. So no renderer was discarding anything \u2014 the contract had no field to discard.",
      "closed_by": "`ExecutionSummary.ending` \u2014 an additive third state (`unstated` / `completed` / `failedConstraint`), omitted from the JSON when unstated so that 'nobody established this' cannot be confused with a claim. The demo generator sets it per tour program from the corpus's own declared capability; `chain/ingest.nim` deliberately leaves every real-chain recording unstated rather than spending the receipt's `revertCode` on it, which is the chain's verdict and the exact conflation the field exists to end. Rendered by `viewutil.executionEndingRow`, added by `txMetadataRows` \u2014 the one producer \u00a77.1 requires \u2014 so the row reaches both surfaces. Unit-covered by `client/tests/test_capability_tour.nim` suite 3a; mutation arm M in tools/journeys/selftest.mjs.",
      "now": "The failing recording's page reads 'Stopped on a failed constraint' where the completing one reads 'Ran to completion'. Both still say the TRANSACTION 'Succeeded', which is correct and is the distinction the row exists to make. 9 of 23 published manifests state an ending \u2014 the nine tour programs, one failing and eight completing \u2014 and the fourteen M5c ones state none."
    },
    "a-positioned-session-says-where-it-is": {
      "was": "Six frozen real-chain sessions published a position (step 128 of 235/345/1315) and never said where it was. They correctly stated that the chain publishes no source and that stepping is at instruction level; they never stated the step. The 108 `.srcline` assertions all ran on the demo chain, so this branch had no assertions at all.",
      "closed_by": "98ccd72 + a490ee3 (`fix/current-line-marker`), landed on dev by a sibling between this branch's first commit and its rebase. `renderPositionHead` now draws 'The session is stopped at step N of M' on the pane that has no line to mark.",
      "now": "7 of 7 positioned sessions with no line to mark state their step; 13 of 13 positioned sessions say where they are."
    },
    "position-survives-hydration": {
      "was": "The hydrated frame marked no line. The served frame showed src/shield.nr with line 32 marked; after hydration the same page showed the same file with 0 lines marked, while the session still published step=128 of 1315. §7.0's 'no state renders less than the pre-hydration page', violated by the product's own bundle.",
      "cause": "`decodeSourceIsland` matched the engine's position path against the bundle's document paths with `==`. The engine reports the path the program was RECORDED at — absolute, belonging to the machine that ran it — and the bundle stores paths relative to the package root, because that is the only form that survives being served to someone else. The comparison was false for every file in every session.",
      "why_it_hid": "The Nargo.toml fix directly above it made an unmatched position CLEAR currentLine rather than carry it onto the wrong document. Correct — and it turned 'marks the wrong line' into 'marks no line', which is also exactly what a bundle that genuinely lacks the file looks like. The safe fallback masked the broken comparison, and only a browser could tell the two apart.",
      "closed_by": "`positionDocumentIndex` in src/debugger/source_island.nim — longest matching path-segment suffix wins. Unit-covered by test_entry_state.nim's eighth suite; mutation arm E in tools/journeys/selftest.mjs.",
      "now": "HYDRATED: one line marked, on screen, in the file the position is in — src/main.nr line 1, 'mod shield;'."
    },
    "stepping-moves-the-position": {
      "was": "A step advanced the URL and moved nothing on the screen. `?t=` went 1 -> 5 and the recovery anchor went main.nr:12 -> main.nr:13, so the click reached the engine and the engine stepped; `data-step` stayed pinned at its landing value and no line was ever marked.",
      "cause": "Two causes, and only the first was shared with the entry above. `data-step` was READ once out of the served DOM (`intAttr(ui.root, \"data-step\")`) and never written again, so the root reported the landing step for the rest of the session. The absent mark was the path-matching defect.",
      "why_it_matters_which": "Verification-Harness-Traps.md §2 in the product rather than in a harness: every success along the way was real. A check written against `location.search` would have been green throughout, which is why journey 03 reads the URL as a CONTROL and takes its verdict from the rendered position.",
      "closed_by": "`renderPanes` now writes `data-step` and `data-total-steps` on every re-render — in the proc that draws the panes, so the attribute and the panes cannot disagree. Mutation arm F.",
      "now": "data-step 0 -> 1 (of 1315), marked line 1 -> 12, exactly one mark after the step."
    }
  }
}
