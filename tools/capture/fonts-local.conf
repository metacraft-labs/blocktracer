<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<!--
  Deterministic text rasterisation inside the pinned capture environment.

  <include>d by the fontconfig that tools/capture/capture-env.nix generates, and
  its SHA-256 is recorded in the environment id — so editing this file moves the
  id and, by design, invalidates every hash stored under the old one.

  These mirror the Chromium flags in lib/determinism.mjs at the fontconfig
  layer, so a face that reaches FreeType directly is rendered the same way as
  one that goes through Skia's own path. Hinting, autohinting, embedded
  bitmaps, subpixel order and the LCD filter are all host-configurable on a
  normal Linux desktop, and every one of them moves glyph pixels.

  This file has no effect on macOS: Chromium there rasterises through
  CoreText and does not link fontconfig at all. It is Linux — where the tier-1
  verdict is produced — that these rules are for.

  Antialiasing stays ON: these images are for a human to review, and turning it
  off would make every capture in the campaign harder to judge in order to make
  a hash marginally easier to reproduce.
-->
<fontconfig>
  <match target="font">
    <edit name="hinting" mode="assign"><bool>false</bool></edit>
    <edit name="hintstyle" mode="assign"><const>hintnone</const></edit>
    <edit name="autohint" mode="assign"><bool>false</bool></edit>
    <edit name="antialias" mode="assign"><bool>true</bool></edit>
    <edit name="rgba" mode="assign"><const>none</const></edit>
    <edit name="lcdfilter" mode="assign"><const>lcdnone</const></edit>
    <edit name="embeddedbitmap" mode="assign"><bool>false</bool></edit>
  </match>
</fontconfig>
