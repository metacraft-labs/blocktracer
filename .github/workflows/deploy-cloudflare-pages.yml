name: Deploy to Cloudflare Pages (reusable)

# ---------------------------------------------------------------------------
# REUSABLE, PARAMETERISED Cloudflare Pages deploy workflow for
# metacraft-labs/blocktracer.
#
# It BUILDS the full deployable static site HERMETICALLY through the repo's Nix
# flake (`nix build .#<flake-target>` -> `result/`) — no sibling checkouts
# required — and PUBLISHES it to a Cloudflare Pages project with wrangler run
# from the repo's flake-pinned `.#ci` dev shell.
#
# WHY this differs from the sibling isonim sites (web-site, reprobuild-web-site):
#   Those render their site in place with `just export` -> `dist/` and hand the
#   reusable a `build-command` + `output-dir`. BlockTracer's FULL deployable
#   site (index.html + the /d data tree + /idx + /t + /aztec entry pages +
#   /registry + assets) is produced by the flake PACKAGE `packages.default`, so
#   this workflow builds with `nix build` and then stages `result/`.
#
#   `result/` is a symlink into the read-only `/nix/store`. wrangler is happiest
#   uploading a plain, writable directory, so we dereference it into a clean
#   staging dir first (`cp -rL result site && chmod -R u+w site`) and deploy
#   THAT. This also sidesteps the two runner facts the sibling sites hit:
#   `wrangler pages deploy` does not honour `.assetsignore`, and the eph-linux
#   Nix runners have no preinstalled node/npx — so we deploy a CLEAN dir with
#   wrangler pinned by flake.lock (`nix develop .#ci`), never an ad-hoc npx.
#
# SINGLE-PROJECT DEPLOYER. This workflow deploys ONE `project-name` per call. It
# does NOT itself know about environments — the caller (deploy.yml) decides
# which Pages project a given branch/PR targets and passes it in `project-name`.
#
# WHY the caller maps branch -> project (the architecture note):
#   A Cloudflare Pages *project custom domain* ALWAYS serves that project's
#   PRODUCTION deployment. You therefore cannot serve `staging.<domain>` /
#   `dev.<domain>` from branch aliases of one project. The proven model is ONE
#   PAGES PROJECT PER ENVIRONMENT, each with its own production branch + custom
#   domain. The caller maps:
#       live    branch -> blocktracer          project (prod branch `live`)
#       staging branch -> blocktracer-staging   project (prod branch `staging`)
#       dev     branch -> blocktracer-dev       project (prod branch `dev`)
#       pull_request   -> blocktracer-dev       project -> native preview alias
#                         `<head-ref>.blocktracer-dev.pages.dev`
#   Cloudflare decides production-vs-preview by comparing the deployed `branch`
#   against the TARGET project's configured production branch (set at project
#   creation with wrangler), so this workflow only deploys the right branch to
#   the right project.
#
# NOTE: this is the Cloudflare PAGES deploy (the demo/static explorer). It is
# independent of the R2 delta-publisher go-live runbook in DEPLOY.md, which
# serves blocktracer.org from an R2 bucket — a separate, credentialed path.
# ---------------------------------------------------------------------------

on:
  workflow_call:
    inputs:
      project-name:
        description: "Cloudflare Pages project to deploy to (create it with wrangler first). The caller computes this per branch/PR."
        required: true
        type: string
      flake-target:
        description: "Flake package to build. `nix build .#<flake-target>` must produce a `result/` that is the complete site."
        required: false
        default: "default"
        type: string
      runner:
        description: "Self-hosted runner label to run on."
        required: false
        default: "eph-linux-x64"
        type: string
    secrets:
      CLOUDFLARE_API_TOKEN:
        description: "Cloudflare API token scoped to Pages:Edit for the metacraft-labs account (covers every per-env project on the account)."
        required: true
      CLOUDFLARE_ACCOUNT_ID:
        description: "Cloudflare account id (metacraft-labs: 803741d99690718276ea30950f690c46)."
        required: true

jobs:
  deploy:
    name: Deploy (${{ inputs.project-name }})
    runs-on: ${{ inputs.runner }}
    # Records a GitHub deployment per branch (live/staging/dev) or `preview` for
    # PRs. Attach protection rules (e.g. required reviewers on `live`) in the
    # repo's Settings -> Environments if desired.
    environment:
      name: ${{ github.event_name == 'pull_request' && 'preview' || github.ref_name }}
      url: ${{ steps.deploy.outputs.url }}
    steps:
      - uses: actions/checkout@v4

      - name: Resolve deploy branch
        # For a PR, use the source branch so the deployment gets its own
        # `<head-ref>.<project>.pages.dev` alias. For a push, use the pushed
        # branch (live/staging/dev). Cloudflare decides production-vs-preview by
        # comparing this to the TARGET project's configured production branch.
        id: vars
        shell: bash
        env:
          # Pass refs through the environment (not inline `${{ }}`) so a crafted
          # branch name cannot inject into the script. See actionlint [expression].
          EVENT_NAME: ${{ github.event_name }}
          HEAD_REF: ${{ github.head_ref }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            echo "branch=$HEAD_REF" >> "$GITHUB_OUTPUT"
          else
            echo "branch=$REF_NAME" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Nix
        uses: metacraft-labs/nixos-modules/.github/setup-nix@main
        with:
          trusted-public-keys: ${{ vars.TRUSTED_PUBLIC_KEYS }}
          substituters: ${{ vars.SUBSTITUTERS }}
          nix-github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build site (hermetic — nix flake package)
        # The repo's flake pins all build inputs (isonim + nim-everywhere +
        # codetracer-design-system as flake inputs), so this needs no sibling
        # checkouts. `flake-target` is a workflow_call input (set by the trusted
        # caller), not attacker-controlled.
        env:
          FLAKE_TARGET: ${{ inputs.flake-target }}
        run: nix build ".#${FLAKE_TARGET}"

      - name: Stage a clean, writable publish dir
        # `result` is a symlink into the read-only /nix/store. wrangler deploys a
        # plain writable directory most reliably, so dereference symlinks
        # (`-L`) into `site/` and restore write bits. `site/` is the CLEAN dir we
        # upload — nothing extraneous, no `.assetsignore` reliance.
        run: |
          set -euo pipefail
          rm -rf site
          cp -rL result site
          chmod -R u+w site
          echo "Staged $(find site -type f | wc -l) files, $(du -sh site | cut -f1) total."

      - name: Vendor the replay engine into the publish dir
        # WHY THIS STEP EXISTS AT ALL
        #
        # Every page this workflow publishes carries
        # `data-replay-engine="/replay-engine/"` and constructs
        # `new Worker("/replay-engine/worker.js", {type:"module"})`. Until this
        # step existed nothing was ever published under that prefix, so the
        # worker 404'd on every deployed page and the debugging controls were
        # inert on a site that otherwise looked complete. The failure was fast
        # (~1 s) and quiet, and it read as "the engine is stale" for hours.
        #
        # WHY VENDORED, AND NOT FETCHED CROSS-ORIGIN AT RUNTIME
        #
        # Not a preference — the alternative does not work as a config change.
        # `client/hydrate/engine_transport.nim`'s `startWorkerImpl` calls
        # `new Worker(url, {type:'module'})`, and a cross-origin script URL
        # there throws `SecurityError`; the transport has a dedicated message
        # for precisely that. Serving the worker from another origin would
        # require a same-origin blob bootstrap (what CodeTracer's own
        # `gateway-client.js` does), i.e. new transport code, not a `-d:` flag.
        # Vendoring is also the CodeTracer-Embed-SDK.md §5.1 recommended
        # "copy to own origin" mode.
        #
        # It additionally pins the page and its engine together. The page
        # speaks a specific DAP dialect (handshake order, `ct/complete-move`,
        # `traceFolder` as the only TraceSource); an engine that moved
        # independently could break a page that did not change. The cost we
        # accept for that: an engine fix now needs a BlockTracer redeploy, and
        # each deploy carries ~18 MB more. Engine releases are far rarer than
        # site deploys, so that is the cheaper side.
        #
        # The build itself stays hermetic: `nix build` cannot reach the
        # network, so the engine is added HERE, to the staged `site/` dir,
        # after the flake package is built and before wrangler uploads it.
        env:
          ENGINE_BASE: ${{ vars.REPLAY_ENGINE_BASE || 'https://web-codetracer.pages.dev' }}
        run: |
          set -euo pipefail
          bash client/hydrate/fetch-engine.sh site/replay-engine "$ENGINE_BASE" \
            .deploy/engine-fetch.json

      - name: Verify the published site can actually start an engine
        # The deploy that caused this whole investigation was green. A green
        # build that publishes a site whose worker 404s must not be able to
        # reach production again, so the invariant is asserted on the bytes
        # that are about to be uploaded rather than trusted.
        run: |
          set -euo pipefail
          missing=0
          for f in replay-engine/worker.js \
                   replay-engine/pkg/db_backend.js \
                   replay-engine/pkg/db_backend_bg.wasm; do
            if [ ! -s "site/$f" ]; then echo "missing/empty: site/$f" >&2; missing=1; fi
          done
          [ "$missing" -eq 0 ] || { echo "the engine is not in the publish dir" >&2; exit 1; }

          # The pages ask for a specific prefix. If someone changes
          # `replayEngineBase`, this catches the mismatch here rather than in a
          # visitor's console.
          page="$(find site -path '*/tx/*/debug/index.html' -print -quit)"
          if [ -n "$page" ]; then
            base="$(grep -oE 'data-replay-engine="[^"]*"' "$page" | head -1 |
                    sed 's/.*="//; s/"$//')"
            echo "pages request their engine from: ${base:-<none>}"
            case "$base" in
              /replay-engine/) echo "  matches what this step published." ;;
              http*|//*)       echo "  cross-origin base — vendored copy is unused." ;;
              *) echo "  ERROR: published engine is at /replay-engine/ but pages ask for '$base'" >&2
                 exit 1 ;;
            esac
          fi
          # The page quotes the engine's size to the visitor
          # (`ReplayEngineWasmBytes` -> `approxMegabytes`). These two numbers
          # are printed here and ASSERTED by check-freshness.mjs's F5 below,
          # at the granularity the page actually renders.
          #
          # The original reasoning for not asserting here was right about the
          # danger and wrong about the conclusion. A byte-exact assertion WOULD
          # make an unrelated engine bump break this repository's deploy — but
          # "report and continue" left a number the page SHOWS A VISITOR
          # unverified for as long as it has existed, and on 2026-09-01 these
          # two lines were read off a deploy log as evidence of a stale publish
          # when in fact they were faithful. A report nobody can act on is not
          # a weaker check than an assertion; it is a different thing that
          # looks like one.
          #
          # F5 asserts `approxMegabytes(declared) == approxMegabytes(actual)`,
          # so a few hundred KB of upstream drift passes exactly as this
          # comment intended, and a drift large enough to make the page tell a
          # visitor the wrong figure fails. The claim asserted is the one the
          # product makes.
          actual="$(wc -c <site/replay-engine/pkg/db_backend_bg.wasm | tr -d ' ')"
          declared="$(grep -oE 'ReplayEngineWasmBytes\* = [0-9_]+' \
                      client/src/debugger/replay_engine.nim |
                      grep -oE '[0-9_]+$' | tr -d '_')"
          echo "engine wasm: vendored ${actual} bytes; page declares ${declared:-<unknown>}"

      - name: Verify every script a published page asks for was published
        # The general form of the step above. `/replay-engine/` was one asset
        # the pages referenced and the publish dir did not contain; the
        # hydration bundle is another that nothing checks, and a `-d:` flag or
        # a build step that stops running would make the pages name a
        # `/assets/hydrate.js` that is not there — served, a session that never
        # becomes live. Neither the Nim suites nor the design gate can see this:
        # they grade the RENDERED MARKUP, and this is a property of the markup
        # AND the file tree together, which only exists here.
        #
        # It is also the check that would have settled the 2026-08-30 hydration
        # investigation in one line instead of the wrong root cause that got
        # committed: the page's reference and the file's presence are asserted
        # against each other, so "the tag is missing" and "the file is missing"
        # can never again be read as each other.
        #
        # Same-origin absolute `src`s only. A cross-origin script is somebody
        # else's to serve, and a relative one is already resolved by the page's
        # own directory, which is not a claim about this tree's layout.
        run: |
          set -euo pipefail
          missing=0
          checked=0
          # `|| true` on both greps: no match is exit 1, and "this site has no
          # same-origin scripts" is a legitimate state to report rather than a
          # pipeline failure to die on.
          refs="$(grep -rhoE '<script[^>]+src="/[^"]*"' site --include='*.html' 2>/dev/null |
                  grep -oE 'src="/[^"]*"' | sed 's/^src="//; s/"$//' | sort -u || true)"
          for ref in $refs; do
            checked=$((checked + 1))
            # Strip any ?query / #fragment before touching the file tree.
            path="site/${ref%%[?#]*}"
            if [ -s "$path" ]; then
              echo "  ok: $ref ($(wc -c <"$path" | tr -d ' ') bytes)"
            else
              echo "  MISSING: pages reference $ref but site${ref} is absent or empty" >&2
              missing=1
            fi
          done
          echo "$checked same-origin script reference(s) resolved against the publish dir."

          # And the positive direction, which is the half an "all references
          # resolve" check cannot cover: a site that references NOTHING passes
          # it vacuously. The debug shell is the page that must hydrate, so it
          # is the one page required to name a script.
          page="$(find site -path '*/tx/*/debug/index.html' -print -quit)"
          if [ -n "$page" ]; then
            if grep -qE '<script[^>]+src="/[^"]*"' "$page"; then
              echo "the debug shell references its bundle: $(grep -oE 'src="/[^"]*\.js"' "$page" | head -1)"
            else
              echo "ERROR: $page carries no same-origin <script> — a session that can never become live" >&2
              missing=1
            fi
          else
            echo "no debug shell in the publish dir; skipping the hydration assertion." >&2
          fi

          [ "$missing" -eq 0 ] || {
            echo "the published pages and the published files do not agree" >&2
            exit 1
          }

      - name: Verify every asset a published page asks for was published
        # THE GENERAL FORM OF BOTH STEPS ABOVE, AND THE ONE THAT COVERS TRACES.
        #
        # The two shell steps before this cover exactly two asset classes: the
        # replay engine, and same-origin `<script src>`. Publishing real chain
        # data adds a third that neither can see — the CodeTracer containers
        # under `/t/**`, which pages name in `data-trace` and the debugger
        # fetches. A container that is missing or truncated fails the same way
        # the engine did: the page renders, the visitor clicks Debug, and
        # nothing happens. Worse, this product's honesty rules mean the page
        # will report the failure accurately, so it looks broken to the visitor
        # rather than to us.
        #
        # `check-assets.mjs` asserts both directions on the staged bytes:
        # every same-origin reference resolves to a NON-EMPTY file, and — the
        # half a resolve-check cannot do — a publish claiming to carry traces
        # must have some page actually name one. `--require-traces` is what
        # makes the second half bite; without it a tree that lost every
        # container would pass by referencing nothing.
        #
        # It runs on `site/`, the staged copy that is about to be uploaded, for
        # the same reason the engine step does: a green build that served a
        # cached store path has reached production here before.
        run: nix develop .#ci --command node tools/deploy/check-assets.mjs --dir site --require-traces

      - name: Record what this commit built and this run fetched
        # EXISTENCE IS NOT FRESHNESS, and every step above this one checks
        # existence. `check-assets.mjs` proves each URL a page names resolves to
        # non-empty bytes; a publish tree carrying LAST WEEK'S `hydrate.js`
        # under the right name passes all of A1–A5, because the reference
        # resolves, the file is not empty, and a page names it.
        #
        # This records the sha256 of every artefact from the two places that
        # can testify to its provenance: `result/`, which is what THIS COMMIT
        # builds, and `.deploy/engine-fetch.json`, which is what THIS RUN
        # downloaded. `site/` is never its own witness — the whole point is to
        # hold the staged tree to something it did not produce.
        run: |
          set -euo pipefail
          nix develop .#ci --command node tools/deploy/check-freshness.mjs record \
            --built result --staged site \
            --engine-fetch .deploy/engine-fetch.json \
            --out .deploy/freshness.json

      - name: Verify the bytes about to be uploaded are the ones this commit builds
        # The pre-upload half. F1 is the assertion the 2026-09-01 investigation
        # wanted and could not make: the staged `assets/hydrate.js` must be
        # byte-identical to the one `nix build` produced from this commit, so a
        # stale bundle fails the deploy instead of being reported in a log
        # nobody reads.
        run: nix develop .#ci --command node tools/deploy/check-freshness.mjs verify \
          --manifest .deploy/freshness.json --dir site

      - name: Deploy to Cloudflare Pages
        id: deploy
        # The self-hosted eph-* runners are Nix runners with no preinstalled
        # node/npx, so the npx-based cloudflare/pages-action and wrangler-action
        # both fail with `npx: command not found`. We do not preinstall software
        # on runners — we run wrangler from the repo's flake-pinned `.#ci`
        # execution environment (wrangler pinned by flake.lock). wrangler
        # authenticates from CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID.
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PROJECT_NAME: ${{ inputs.project-name }}
          DEPLOY_BRANCH: ${{ steps.vars.outputs.branch }}
        run: |
          set -euo pipefail
          nix develop .#ci --command wrangler pages deploy site \
            --project-name="$PROJECT_NAME" \
            --branch="$DEPLOY_BRANCH" \
            --commit-dirty=true | tee wrangler-deploy.log
          url="$(grep -oE 'https://[a-z0-9.-]+\.pages\.dev' wrangler-deploy.log | tail -1 || true)"
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "Deployed to: $url"

      - name: Verify the ORIGIN serves the bytes this run published
        # THE HALF NO GATE IN THIS REPOSITORY HAS EVER PERFORMED.
        #
        # Every check above — the engine step, the script step,
        # `check-assets.mjs`, and even F1 — reads `site/`, the bytes this
        # deploy INTENDS to upload. Nothing has ever read back what the origin
        # ends up serving. `wrangler pages deploy` uploads by content hash and
        # skips what it believes the project already holds, so "the upload
        # succeeded" is a claim about the upload and not about the origin, and
        # the difference between those two is precisely the question "can a
        # deploy silently publish a stale artefact?" that nothing could answer.
        #
        # It fetches the manifest's critical set from the deployment URL
        # wrangler just printed and compares sha256s. F6b asserts the set is
        # not vacuous, because "all 0 of 0 artefacts match" is green and worth
        # nothing.
        #
        # RETRIED, because Cloudflare's edge takes a few seconds to serve a new
        # deployment and a propagation delay is not a stale artefact. Five
        # attempts over ~40 s, and the failure after that is a real one — the
        # last attempt's output is what lands in the log.
        if: steps.deploy.outputs.url != ''
        env:
          DEPLOY_URL: ${{ steps.deploy.outputs.url }}
        run: |
          set -uo pipefail
          for attempt in 1 2 3 4 5; do
            echo "--- attempt ${attempt} against ${DEPLOY_URL}"
            if nix develop .#ci --command node tools/deploy/check-freshness.mjs verify \
                 --manifest .deploy/freshness.json --origin "$DEPLOY_URL"; then
              exit 0
            fi
            [ "${attempt}" -lt 5 ] && sleep 10
          done
          echo "the origin is not serving what this run published" >&2
          exit 1
